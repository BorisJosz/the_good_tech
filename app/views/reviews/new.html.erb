<h1>Add a new review</h1>

<div class="container">
<h1>Let's make a new review</h1>
<%= simple_form_for [@user, @booking, @review] do |f| %>
  <%= f.input :description %>
  <%= f.input :stars %>
  <%= f.submit "add review", class: "btn btn-primary" %>
<% end %>
</div>

