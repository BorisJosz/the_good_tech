<div class="banner">
</div>

<h1>Our Selection :</h1>
<br>
<br>
<div class="container">
  <div class="row">
    <% @categories.each do |category| %>
    <div class="col-xs-12 col-sm-6 col-lg-4">
      <div class="category-card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('assets/<%= category %>.png');">
        <%= link_to category, root_path(filter: category), class: "category-link" %>
      </div>
    </div>
    <% end %>
  </div>
</div>

<div class="row">

<% unless params[:filter].nil? %>
<% @items.each do |item| %>
<%= link_to item_path(item) do %>

<div class="col-xs-12 col-sm-3">
  <div class="card indexcard">
    <div>
      <%= cl_image_tag item.photo, class:"indeximage" %>
    </div>
    <div class = "text-center">
    <h5 class= "indexlink"><%= item.title.upcase %></h5>
    <p class= "indexlink"><%= item.description %></p>
    </div>
 </div>
</div>
<% end %>
   <% end %>

<% end %>
  </div>


